# https://taskfile.dev

version: "3"

tasks:
  proxmox-i:
    cmd: ansible-playbook proxmox/main.yml -i proxmox/inventory.yml --tags install

  proxmox-create:
    cmd: ansible-playbook proxmox/main.yml -i proxmox/inventory.yml --tags create
