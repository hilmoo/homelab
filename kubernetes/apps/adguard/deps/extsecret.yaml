apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: adguard-secrets
spec:
  refreshInterval: "0"
  secretStoreRef:
    name: bitwarden-secret-store
    kind: ClusterSecretStore
  data:
    - secretKey: TS_AUTHKEY
      remoteRef:
        key: "cef8bc9c-757f-469d-8bec-b3bc002ee4ac"
