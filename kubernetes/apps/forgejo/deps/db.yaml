apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: manual-forgejo-db
provisioner: kubernetes.io/no-provisioner
volumeBindingMode: WaitForFirstConsumer
---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: forgejo-db-pv-manual
  labels:
    type: local
    app.kubernetes.io/name: forgejo-db
spec:
  storageClassName: manual-forgejo-db
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /mnt/data/forgejo/db
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: type
              operator: In
              values:
                - agent-a
