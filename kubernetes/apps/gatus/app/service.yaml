apiVersion: v1
kind: Service
metadata:
  name: &app gatus
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: *app
    app.kubernetes.io/instance: *app
  ports:
    - name: http
      port: 8080
      protocol: TCP
      targetPort: 8080
