apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: manual-keycloak-db
provisioner: kubernetes.io/no-provisioner
volumeBindingMode: WaitForFirstConsumer
---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: keycloak-db-pv-manual
  labels:
    type: local
    app.kubernetes.io/name: keycloak-db
spec:
  storageClassName: manual-keycloak-db
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /mnt/data/keycloak/db
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: type
              operator: In
              values:
                - agent-a
