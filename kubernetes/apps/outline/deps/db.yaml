apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: manual-outline-db
provisioner: kubernetes.io/no-provisioner
volumeBindingMode: WaitForFirstConsumer
---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: outline-db-pv-manual
  labels:
    type: local
    app.kubernetes.io/name: outline-db
spec:
  storageClassName: manual-outline-db
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /mnt/data/outline/db
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: type
              operator: In
              values:
                - agent-a
