apiVersion: ceph.rook.io/v1
kind: CephObjectStore
metadata:
  name: store
spec:
  metadataPool:
    failureDomain: host
    replicated:
      size: 1
  dataPool:
    failureDomain: host
    replicated:
      size: 1
  preservePoolsOnDelete: true
  gateway:
    port: 80
    instances: 1
