apiVersion: v1
kind: Service
metadata:
  name: &app vaultwarden
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: *app
    app.kubernetes.io/instance: *app
  ports:
    - name: web
      port: 80
      targetPort: 80
