---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: vaultwarden-deps
  namespace: flux-system
spec:
  targetNamespace: vaultwarden
  interval: 2h
  path: ./kubernetes/apps/vaultwarden/deps
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  dependsOn:
    - name: external-secrets-store
