apiVersion: v1
kind: Service
metadata:
  name: &app my-catatan-kuliah
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: *app
    app.kubernetes.io/instance: *app
  ports:
    - port: 8080
      targetPort: 8080
      name: http
