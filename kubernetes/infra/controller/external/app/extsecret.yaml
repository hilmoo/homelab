apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: caddy-secrets
spec:
  refreshInterval: 24h
  secretStoreRef:
    name: bitwarden-secret-store
    kind: ClusterSecretStore
  data:
    - secretKey: Caddyfile
      remoteRef:
        key: "9cd32bc4-427b-4f52-9437-b3bb0176dc0f"
